{
  "name": "root",
  "private": true,
  "scripts": {
    "init": "npx lerna bootstrap --use-workspaces",
    "start:app": "cd ./packages/app && npm start",
    "build": "npx lerna run build"
  },
  "devDependencies": {
    "lerna": "^5.5.2"
  },
  "workspaces": [
    "packages/*"
  ]
}
