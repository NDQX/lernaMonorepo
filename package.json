{
  "name": "root",
  "private": true,
  "scripts": {
    "init": "lerna bootstrap --use-workspaces",
    "start:app": "cd ./packages/app && npm start",
    "build": "lerna run build",
    "clean": "lerna run clean"
  },
  "devDependencies": {
    "lerna": "^5.5.2"
  },
  "workspaces": [
    "packages/*"
  ]
}
